{
  "name": "snakeumms",
  "version": "1.0.0",
  "private": true,
  "author": "PyOfile",
  "license": "MIT",
  "scripts": {
    "serv":"nodemon backend/server",
    "cli": "cd frontend && yarn go",
    "dev":"concurrently \"yarn serv\" \"yarn cli\"",
    "p1": "sh ~/.bash/bin/gitpull.sh",
    "p2": "sh ~/.bash/bin/gitpush.sh",
    "build":"yarn && cd frontend && yarn",
    "burn":"sudo rm -r node_modules && cd frontend && sudo rm -r node_modules",
    "light": "lighthouse http://localhost:5050/ --view"
  },
  "devDependencies": {
    "nodemon":"^2.0.7",
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "socket.io": "^3.1.0"
  }
}
